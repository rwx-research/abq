The certificate in this folder are used for integration tests in
`crates/abq_cli/tests`.

The certifiacte expires October 4, 2025.

See `development.md` `SSL Certificates` for how to regenerate these.
